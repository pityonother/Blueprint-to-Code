# Auto detect text files and perform LF normalization
* text=auto

# Publish the sanitized ARK discovery review bundle without placing 500+ MB in Git history.
knowledge_base/discovery_bundle.zip filter=lfs diff=lfs merge=lfs -text
